// src/firebase/config.js
// Firebase has been fully replaced by JWT auth + MySQL backend.
// Kept as stub to avoid breaking imports.
export const db   = null
export const auth = null
export default null
